# 标签

## i

`<i>` 标签在 **HTML** 里最早是用来表示 *italic*（斜体）的，也就是让文字倾斜显示。

不过在 **HTML5** 规范中，它的语义已经扩展了，不仅仅是“斜体”，而是用来表示 **一段与周围文本在语义上有所不同的文字**，例如：

* 外来词（如拉丁文术语）
* 技术术语
* 思想/内心独白
* 舞台指令
* 图标（很多 UI 库里用 `<i>` 搭配 CSS class 来显示 icon）
